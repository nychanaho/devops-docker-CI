# The deployment section should be excluded until
# it is explicitly included in module 5 done again again and again

deployment:
  dockerhub:
    branch: master
    commands:
      - curl --header "Content-Type:application/json"  --data '{"build": true}' --request POST $DOCKER_HUB_URL

test:
  override:    
    - mocha