# The deployment section should be excluded until
# it is explicitly included in module 5 done again again and again and again and again

deployment:
  dockerhub:
    branch: master
    commands:
      - curl -H "Content-Type: application/json" --data '{"docker_tag": "master"}' -X POST $DOCKER_HUB_URL

test:
  override:    
    - mocha